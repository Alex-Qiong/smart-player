# ---- 播放器核心 ----
PySide6>=6.6            # GUI
python-mpv>=0.5         # libmpv 绑定（另需 libmpv-2.dll，见 tools/fetch_libmpv.py）
imageio-ffmpeg>=0.5     # 自带 ffmpeg 二进制，免安装

# ---- 本地 AI：语音识别（ASR）----
faster-whisper>=1.0     # 本地 Whisper，CTranslate2 后端
numpy>=1.26

# ---- 本地 AI：字幕翻译 ----
argostranslate>=1.9     # 离线神经机器翻译

# ---- 本地 AI：TTS 配音（可选）----
piper-tts>=1.2           # 本地 ONNX 语音合成

# ---- 模型下载工具 ----
huggingface-hub>=0.20   # 下载 faster-whisper 模型
